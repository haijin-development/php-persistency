language: php
php:
  - '7.1'
before_script:
  - composer self-update
  - composer install
script: ./vendor/bin/phpunit tests/
services:
  - mysql
before_install:
  - mysql -u root --password="" < tests/travis.sql