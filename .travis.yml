language: php
php:
  - '7.1'
before_script:
  - composer self-update
  - composer install
script: composer specs
services:
  - mysql
  - postgresql
  - sqlite3
before_install:
  - mysql -u root --password="" < tests/travis-mysql.sql